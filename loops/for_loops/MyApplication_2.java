public class MyApplication {
	public static void main(String[]args){
		

	for(int i=0;i<10;i++) {  // this format will always return true if you leave the paranthesis empty. To enable control of the loop you need to (intialize a variable; create a condition such as setting a threshold; and adding an update section where the value inside the variable changes on every iteration until we have a false condition.

			System.out.printf("Format specifier contains a special character, begins with percentage sign, following with a letter. Integer values use the d letter : %d.\n", i);


			}

		} 

	}
